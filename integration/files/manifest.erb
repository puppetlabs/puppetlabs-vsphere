vsphere_vm { '<%= path %>':
  ensure          => <%= status %>,
  source          => '<%= source_path %>',
  memory          => '<%= memory %>',
  cpus            =>  '<%= cpus %>',
  template        => '<%= is_template %>',
  annotation      => '<%= annotation %>',
}
