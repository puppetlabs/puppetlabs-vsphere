vsphere_vm { '<%= path %>':
  ensure          => <%= status %>,
  source          => '<%= source_path %>',
  memory          => <%= memory %>,
  cpus            => <%= cpu %>,
  template        => '<%= is_template %>',
  annotation      => '<%= annotation %>',
}
