vsphere_vm { '<%= path %>':
  ensure          => <%= status %>,
  source          => '<%= source_path %>',
  memory          => 512,
  cpus            => 1,
  template        => '<%= is_template %>',
}
